import "@/styles/globals.css";

const Page = () => {
  return (
    <h1 className={ `font-8xl` }>
      compliers vs <span className={ `font-[1px]` }>interpreters</span>
    </h1>
  )
}

export default Page